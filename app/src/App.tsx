import SpotifyInterface from "./features/components/spotify-interface";

const App = () => {
  return (
    <main className="h-screen w-screen overflow-hidden bg-black">
      <SpotifyInterface />
    </main>
  );
};

export default App;
